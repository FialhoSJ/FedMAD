# SLMAggregatorAgent - Dependências para Phi-3-mini
# Instalar com: pip install -r requirements.txt
transformers>=4.40.0
torch>=2.1.0
accelerate
bitsandbytes  # necessário apenas para GPU (quantização 4-bit)
